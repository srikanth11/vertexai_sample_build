# steps:
# - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
#   args:
#   - gcloud
#   - functions
#   - call
#   - function-1
#   - --region="us-central1"
#   - --source=.
#   - --entry-point=hello_gcs
#   - --trigger-http
#   - --runtime=python310

steps:
  - name: gcr.io/google.com/cloudsdktool/cloud-sdk
    args:
      - gcloud
      - functions
      - call
      - function-1
      - --region
      - us-central1
      - --data
      - '{}'
options:
  logging: CLOUD_LOGGING_ONLY

